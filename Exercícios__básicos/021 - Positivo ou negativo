#include <stdio.h>

int main()
{
    int numero;
    
    printf("Digite um numero negativo ou positivo: ");
    scanf ("%d", &numero);
    
    if (numero>0) printf ("O numero %d é positivo", numero);
    if (numero<0) printf ("O numero %d é negativo", numero);
    
    return 0;
}
