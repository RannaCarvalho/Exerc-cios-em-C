/*algoritmo que leia 2 valores (x e y), que devem representar as coordenadas de um ponto em um plano. 
A seguir, determine qual o quadrante ao qual pertence o ponto, ou se está sobre um dos eixos cartesianos ou na origem (x=y=0)*/
#include <stdio.h>
#include <locale.h>

int main()
{
    setlocale(LC_ALL, "Portuguese");
    float x, y;
    
    printf("Informe o valor de x: ");
    scanf ("%f", &x);
    printf("Informe o valor de y: ");
    scanf ("%f", &y);
    
    if (x>0 && y>0) printf ("o ponto está no primeiro quadrante");
    else if (x<0 && y>0) printf ("o ponto está no segundo quadrante");
    else if (x<0 && y<0) printf ("o ponto está no terceiro quadrante");
    else if (x>0 && y<0) printf ("o ponto está no quarto quadrante");
    else if (x==0 && y!=0) printf ("o ponto está no eixo y");
    else printf ("o ponto está no eixo x");

    return 0;
}
