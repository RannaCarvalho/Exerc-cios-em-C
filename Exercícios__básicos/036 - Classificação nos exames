/*Em uma certificação são feitos são feitos 5 exames (I, II, III, IV e V). Escreva um
programa que leia as notas destes exames e imprima a classificação do aluno, sabendo que a média é 70.
Classificação: A – passou em todos os exames; B – passou em I, II e IV, mas não em III ou V;
C – passou em I e II, III ou IV, mas não em V; Reprovado – outras situações.*/
#include <stdio.h>
#include <locale.h>

int main()
{
    setlocale(LC_ALL, "Portuguese");
    float notas[5];
    int i;
    
    for (i=0; i<5; i++){
        printf("Informe a nota do exame %d: ", i+1);
        scanf ("%f", &notas[i]);}
    printf("\n");
    for (i=0; i<5; i++){
        if (notas[0]>=70 && notas[1]>=70 && notas[2]>=70 && notas[3]>=70 && notas[4]>=70){
            printf ("Classificação: A");break;}
        else if (notas[0]>=70 && notas[1]>=70 && notas[3]>=70 && (notas[2]<70 || notas[4]<70){
            printf ("Classificação: B");break;}
        else if (notas[0]>=70 && notas[1]>=70 && (notas[2]>=70 || notas[3]>=70) && notas[4]<70){
            printf ("Classificação: C");break;}
        else{
            printf("REPROVADO");break;}
    }
    
    return 0;
}
